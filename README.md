# cost-analyzer-on-aws
This project aimed to analyse the usage and cost of the services in aws and display the details static website in the s3 and also it monitor the  cost using the cloudwatch metrics if the amount exceeds it send the email notification through the SNS to the user 
